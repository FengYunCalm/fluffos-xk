{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "fluffos.evidence.manifest.v1",
  "title": "FluffOS benchmark/runtime evidence manifest schema",
  "description": "Required metadata for every machine-readable benchmark, smoke, or capacity report produced from this repository. Any report used for runtime acceptance must carry these fields; a report missing a required field fails the evidence check.",
  "type": "object",
  "required": [
    "schema",
    "run_id",
    "commit_sha",
    "build_config_hash",
    "compiler",
    "platform",
    "workload_version",
    "started_at",
    "ended_at",
    "command",
    "cleanup"
  ],
  "properties": {
    "schema": {
      "const": "fluffos.evidence.manifest.v1"
    },
    "run_id": {
      "type": "string",
      "description": "Unique run identifier, e.g. generated by the runner script."
    },
    "commit_sha": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$",
      "description": "Full git commit SHA the report was produced against."
    },
    "build_config_hash": {
      "type": "string",
      "description": "Hash of the CMake configure options used (CMAKE_BUILD_TYPE, sanitizer/LTO/MARCH flags)."
    },
    "compiler": {
      "type": "object",
      "required": [
        "name",
        "version"
      ],
      "properties": {
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        }
      },
      "description": "Compiler identity and version string."
    },
    "platform": {
      "type": "object",
      "required": [
        "os",
        "arch",
        "cpu_cores"
      ],
      "properties": {
        "os": {
          "type": "string"
        },
        "arch": {
          "type": "string"
        },
        "cpu_cores": {
          "type": "integer"
        }
      },
      "description": "Runtime platform fingerprint."
    },
    "workload_version": {
      "type": "string",
      "description": "Version identifier of the workload/scenario definition, so runs can be compared only within the same workload version."
    },
    "started_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 run start time (UTC)."
    },
    "ended_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 run end time (UTC)."
    },
    "command": {
      "type": "string",
      "description": "Exact command line used to produce the report."
    },
    "cleanup": {
      "type": "object",
      "required": [
        "state"
      ],
      "properties": {
        "state": {
          "enum": [
            "clean",
            "partial",
            "failed",
            "unknown"
          ],
          "description": "Whether queues/claims/refs returned to baseline after the run."
        },
        "detail": {
          "type": "string"
        }
      }
    },
    "evidence_kind": {
      "type": "string",
      "enum": [
        "historical",
        "current",
        "external-required",
        "unknown"
      ],
      "description": "Classification of the evidence. historical: produced on an earlier checkout; current: produced on the current commit; external-required: requires an environment outside this repository."
    },
    "tested_sha": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$",
      "description": "Actual SHA checked out and built for the run (equals commit_sha for local runs; merge SHA on pull_request CI)."
    },
    "source_sha": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$",
      "description": "PR head SHA for pull_request runs; equals tested_sha otherwise."
    },
    "raw_report": {
      "type": "string",
      "pattern": "^[^/\\\\]+$",
      "description": "File name of the raw benchmark JSON this envelope wraps (same directory as the envelope)."
    },
    "raw_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 of the raw report bound by this envelope."
    }
  },
  "additionalProperties": true
}